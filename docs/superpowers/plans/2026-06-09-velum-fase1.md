# Velum — Fase 1 (Core MVP) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Build a working production order management system for VELUM with cascade engine, operator mobile view, and supervisor real-time dashboard.

**Architecture:** Next.js 14 App Router with server-side route handlers, Prisma ORM over Supabase PostgreSQL, Supabase Realtime for WebSocket push to supervisor dashboard. Business logic lives in pure service functions (testable with Vitest); route handlers are thin wrappers.

**Tech Stack:** Next.js 14 · TypeScript · Tailwind CSS · Prisma 5 · Supabase (PostgreSQL + Realtime + Auth) · Vitest · Vercel (deploy)

---

## File Map

```
velum/
├── prisma/
│   ├── schema.prisma
│   └── seed.ts
├── src/
│   ├── app/
│   │   ├── layout.tsx
│   │   ├── page.tsx
│   │   ├── (auth)/login/page.tsx
│   │   ├── (operario)/
│   │   │   ├── layout.tsx
│   │   │   └── page.tsx                  ← machine selector + active orders
│   │   ├── (supervisor)/
│   │   │   ├── layout.tsx
│   │   │   └── dashboard/page.tsx        ← real-time cascade dashboard
│   │   └── api/
│   │       ├── proyectos/route.ts
│   │       ├── proyectos/[id]/route.ts
│   │       ├── ordenes/route.ts
│   │       ├── ordenes/[id]/route.ts
│   │       ├── ordenes/[id]/progreso/route.ts
│   │       ├── maquinas/route.ts
│   │       └── maquinas/[id]/route.ts
│   ├── components/
│   │   ├── operario/OrdenCard.tsx
│   │   ├── operario/RegistrarAvanceForm.tsx
│   │   ├── supervisor/OrdenCascadaCard.tsx
│   │   └── supervisor/MaquinasStatus.tsx
│   ├── lib/
│   │   ├── prisma.ts
│   │   ├── supabase-server.ts
│   │   ├── supabase-browser.ts
│   │   ├── cascada.ts                    ← pure cascade engine
│   │   └── services/
│   │       ├── ordenes.ts
│   │       ├── proyectos.ts
│   │       └── maquinas.ts
│   ├── middleware.ts
│   └── types/index.ts
└── tests/
    ├── lib/cascada.test.ts
    └── lib/services/ordenes.test.ts
```

---

## Task 1: Project Bootstrap

**Files:**
- Create: `package.json`, `next.config.js`, `vitest.config.ts`, `.env.local.example`

- [ ] **Step 1: Scaffold Next.js 14**

```bash
cd C:\Users\Nissei\Velum
npx create-next-app@14 app --typescript --tailwind --eslint --app --src-dir --no-import-alias
```

When prompted: App Router → Yes, `src/` directory → Yes, import alias → No.

- [ ] **Step 2: Install dependencies**

```bash
cd app
npm install @supabase/supabase-js @supabase/ssr @prisma/client
npm install -D prisma vitest @vitejs/plugin-react vite-tsconfig-paths
```

- [ ] **Step 3: Configure Vitest**

Create `vitest.config.ts`:

```typescript
import { defineConfig } from 'vitest/config'
import tsconfigPaths from 'vite-tsconfig-paths'

export default defineConfig({
  plugins: [tsconfigPaths()],
  test: {
    environment: 'node',
    globals: true,
  },
})
```

Add to `package.json` scripts:
```json
"test": "vitest",
"test:run": "vitest run"
```

- [ ] **Step 4: Create `.env.local.example`**

```bash
NEXT_PUBLIC_SUPABASE_URL=https://xxxx.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key
DATABASE_URL=postgresql://postgres:[password]@db.xxxx.supabase.co:5432/postgres
DIRECT_URL=postgresql://postgres:[password]@db.xxxx.supabase.co:5432/postgres
```

Copy to `.env.local` and fill in values from Supabase dashboard → Settings → API.

- [ ] **Step 5: Verify setup**

```bash
npm run dev
```

Expected: Next.js app running on http://localhost:3000

- [ ] **Step 6: Commit**

```bash
git add .
git commit -m "feat: bootstrap Next.js 14 + Prisma + Supabase + Vitest"
```

---

## Task 2: Prisma Schema

**Files:**
- Create: `prisma/schema.prisma`

- [ ] **Step 1: Initialize Prisma**

```bash
npx prisma init
```

- [ ] **Step 2: Write schema**

Replace `prisma/schema.prisma`:

```prisma
generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider  = "postgresql"
  url       = env("DATABASE_URL")
  directUrl = env("DIRECT_URL")
}

enum Rol {
  OPERARIO
  SUPERVISOR
  GERENCIA
}

enum TipoMaquina {
  LASER
  PUNZONADORA_CNC
  FRESADORA
  EXPANSORA
  PLEGADORA
  LAVADO
  PINTADO
  HORNO
  EMBALAJE
  DESPACHO
}

enum EstadoMaquina {
  OPERATIVA
  MANTENIMIENTO
  FUERA_DE_SERVICIO
}

enum Unidad {
  PIEZAS
  METROS
  M2
}

enum EstadoOrden {
  PENDIENTE
  EN_PRODUCCION
  EN_ESPERA
  COMPLETADA
  CANCELADA
}

enum EstadoEjecucion {
  PENDIENTE
  ACTIVA
  EN_ESPERA
  COMPLETADA
}

enum EstadoProyecto {
  ACTIVO
  COMPLETADO
  CANCELADO
}

model Usuario {
  id        String   @id @default(cuid())
  nombre    String
  email     String   @unique
  rol       Rol
  createdAt DateTime @default(now())

  ejecuciones EjecucionEtapa[]
  registros   RegistroProgreso[]
}

model Proyecto {
  id           String         @id @default(cuid())
  nombre       String
  cliente      String
  fechaEntrega DateTime
  estado       EstadoProyecto @default(ACTIVO)
  createdAt    DateTime       @default(now())

  ordenes OrdenProduccion[]
}

model Maquina {
  id           String        @id @default(cuid())
  nombre       String
  tipo         TipoMaquina
  estadoActual EstadoMaquina @default(OPERATIVA)

  etapasRuta  EtapaRuta[]
  ejecuciones EjecucionEtapa[]
}

model Ruta {
  id          String  @id @default(cuid())
  sistema     String
  producto    String
  descripcion String?

  etapas  EtapaRuta[]
  ordenes OrdenProduccion[]

  @@unique([sistema, producto])
}

model EtapaRuta {
  id               String @id @default(cuid())
  rutaId           String
  maquinaId        String
  ordenSecuencia   Int
  nombreEtapa      String
  umbralActivacion Float  @default(100)

  ruta        Ruta             @relation(fields: [rutaId], references: [id])
  maquina     Maquina          @relation(fields: [maquinaId], references: [id])
  ejecuciones EjecucionEtapa[]
}

model OrdenProduccion {
  id               String      @id @default(cuid())
  proyectoId       String?
  rutaId           String
  sistema          String
  producto         String
  cantidad         Float
  unidad           Unidad
  porcentajeGlobal Float       @default(0)
  estado           EstadoOrden @default(PENDIENTE)
  prioridad        Int         @default(0)
  createdAt        DateTime    @default(now())

  proyecto    Proyecto?        @relation(fields: [proyectoId], references: [id])
  ruta        Ruta             @relation(fields: [rutaId], references: [id])
  ejecuciones EjecucionEtapa[]
}

model EjecucionEtapa {
  id               String          @id @default(cuid())
  ordenId          String
  etapaRutaId      String
  maquinaId        String
  operarioId       String?
  porcentajeActual Float           @default(0)
  estado           EstadoEjecucion @default(PENDIENTE)
  fechaInicio      DateTime?
  fechaFin         DateTime?
  createdAt        DateTime        @default(now())

  orden      OrdenProduccion    @relation(fields: [ordenId], references: [id])
  etapaRuta  EtapaRuta          @relation(fields: [etapaRutaId], references: [id])
  maquina    Maquina            @relation(fields: [maquinaId], references: [id])
  operario   Usuario?           @relation(fields: [operarioId], references: [id])
  registros  RegistroProgreso[]
}

model RegistroProgreso {
  id                   String   @id @default(cuid())
  ejecucionEtapaId     String
  usuarioId            String
  porcentajeRegistrado Float
  cantidadRegistrada   Float
  timestamp            DateTime @default(now())
  notas                String?
  fueOverride          Boolean  @default(false)
  motivoOverride       String?

  ejecucionEtapa EjecucionEtapa @relation(fields: [ejecucionEtapaId], references: [id])
  usuario        Usuario        @relation(fields: [usuarioId], references: [id])
}
```

- [ ] **Step 3: Run migration**

```bash
npx prisma migrate dev --name init
```

Expected: Migration created and applied. Tables visible in Supabase dashboard → Table Editor.

- [ ] **Step 4: Generate Prisma client**

```bash
npx prisma generate
```

- [ ] **Step 5: Commit**

```bash
git add prisma/
git commit -m "feat: add Prisma schema with all Velum models"
```

---

## Task 3: Seed Data — Máquinas y Rutas

**Files:**
- Create: `prisma/seed.ts`

> **ACCIÓN REQUERIDA POR VELUM:** Completar todas las rutas en el array `RUTAS` con las máquinas reales de cada Sistema+Producto. El formato está documentado abajo con ejemplos.

- [ ] **Step 1: Write seed file**

Create `prisma/seed.ts`:

```typescript
import { PrismaClient, TipoMaquina } from '@prisma/client'

const prisma = new PrismaClient()

const MAQUINAS = [
  { nombre: 'Láser',           tipo: TipoMaquina.LASER },
  { nombre: 'Punzonadora CNC', tipo: TipoMaquina.PUNZONADORA_CNC },
  { nombre: 'Fresadora',       tipo: TipoMaquina.FRESADORA },
  { nombre: 'Expansora',       tipo: TipoMaquina.EXPANSORA },
  { nombre: 'Plegadora',       tipo: TipoMaquina.PLEGADORA },
  { nombre: 'Lavado',          tipo: TipoMaquina.LAVADO },
  { nombre: 'Pintado',         tipo: TipoMaquina.PINTADO },
  { nombre: 'Horno',           tipo: TipoMaquina.HORNO },
  { nombre: 'Embalaje',        tipo: TipoMaquina.EMBALAJE },
  { nombre: 'Despacho',        tipo: TipoMaquina.DESPACHO },
]

// Formato de una ruta:
// { sistema, producto, etapas: [{ maquinaNombre, ordenSecuencia, nombreEtapa, umbralActivacion }] }
// umbralActivacion: % que debe alcanzar esta etapa para habilitar la siguiente (100 = completar primero)

const RUTAS = [
  // ── SHEET ──────────────────────────────────────────────────────────────
  {
    sistema: 'Sheet', producto: 'MultiSlim.A',
    etapas: [
      { maquinaNombre: 'Punzonadora CNC', ordenSecuencia: 1, nombreEtapa: 'Punzonado',   umbralActivacion: 50 },
      { maquinaNombre: 'Plegadora',       ordenSecuencia: 2, nombreEtapa: 'Plegado',     umbralActivacion: 40 },
      { maquinaNombre: 'Lavado',          ordenSecuencia: 3, nombreEtapa: 'Lavado',      umbralActivacion: 30 },
      { maquinaNombre: 'Pintado',         ordenSecuencia: 4, nombreEtapa: 'Pintado',     umbralActivacion: 20 },
      { maquinaNombre: 'Horno',           ordenSecuencia: 5, nombreEtapa: 'Curado',      umbralActivacion: 100 },
      { maquinaNombre: 'Embalaje',        ordenSecuencia: 6, nombreEtapa: 'Embalaje',   umbralActivacion: 100 },
      { maquinaNombre: 'Despacho',        ordenSecuencia: 7, nombreEtapa: 'Despacho',   umbralActivacion: 100 },
    ],
  },
  {
    sistema: 'Sheet', producto: 'StandardFlat',
    etapas: [
      // TODO VELUM: completar con las máquinas reales de Sheet/StandardFlat
      { maquinaNombre: 'Láser',    ordenSecuencia: 1, nombreEtapa: 'Corte láser', umbralActivacion: 50 },
      { maquinaNombre: 'Plegadora',ordenSecuencia: 2, nombreEtapa: 'Plegado',    umbralActivacion: 40 },
      { maquinaNombre: 'Pintado',  ordenSecuencia: 3, nombreEtapa: 'Pintado',    umbralActivacion: 20 },
      { maquinaNombre: 'Horno',    ordenSecuencia: 4, nombreEtapa: 'Curado',     umbralActivacion: 100 },
      { maquinaNombre: 'Embalaje', ordenSecuencia: 5, nombreEtapa: 'Embalaje',  umbralActivacion: 100 },
      { maquinaNombre: 'Despacho', ordenSecuencia: 6, nombreEtapa: 'Despacho',  umbralActivacion: 100 },
    ],
  },
  // ── SKIN ───────────────────────────────────────────────────────────────
  {
    sistema: 'Skin', producto: 'Standard',
    etapas: [
      // TODO VELUM: completar con las máquinas reales de Skin/Standard
    ],
  },
  // ... agregar las ~77 rutas restantes con el mismo formato
]

async function main() {
  console.log('Seeding machines...')
  const maquinasCreadas: Record<string, string> = {}
  for (const m of MAQUINAS) {
    const maquina = await prisma.maquina.upsert({
      where: { id: m.tipo },
      update: {},
      create: { id: m.tipo, nombre: m.nombre, tipo: m.tipo },
    })
    maquinasCreadas[m.nombre] = maquina.id
  }

  console.log('Seeding routes...')
  for (const r of RUTAS) {
    if (r.etapas.length === 0) continue
    const ruta = await prisma.ruta.upsert({
      where: { sistema_producto: { sistema: r.sistema, producto: r.producto } },
      update: {},
      create: {
        sistema: r.sistema,
        producto: r.producto,
        etapas: {
          create: r.etapas.map(e => ({
            maquinaId:         maquinasCreadas[e.maquinaNombre],
            ordenSecuencia:    e.ordenSecuencia,
            nombreEtapa:       e.nombreEtapa,
            umbralActivacion:  e.umbralActivacion,
          })),
        },
      },
    })
    console.log(`  ✓ ${ruta.sistema}/${ruta.producto}`)
  }
  console.log('Seed complete.')
}

main().catch(console.error).finally(() => prisma.$disconnect())
```

- [ ] **Step 2: Add seed script to `package.json`**

```json
"prisma": {
  "seed": "ts-node --compiler-options {\"module\":\"CommonJS\"} prisma/seed.ts"
}
```

Install ts-node:
```bash
npm install -D ts-node
```

- [ ] **Step 3: Run seed**

```bash
npx prisma db seed
```

Expected output: `✓ Sheet/MultiSlim.A`, `✓ Sheet/StandardFlat`, etc.

- [ ] **Step 4: Commit**

```bash
git add prisma/seed.ts package.json
git commit -m "feat: add seed data for machines and routes"
```

---

## Task 4: Prisma Client + Supabase Clients

**Files:**
- Create: `src/lib/prisma.ts`, `src/lib/supabase-server.ts`, `src/lib/supabase-browser.ts`

- [ ] **Step 1: Prisma singleton**

Create `src/lib/prisma.ts`:

```typescript
import { PrismaClient } from '@prisma/client'

const globalForPrisma = globalThis as unknown as { prisma: PrismaClient }

export const prisma =
  globalForPrisma.prisma ?? new PrismaClient({ log: ['error'] })

if (process.env.NODE_ENV !== 'production') globalForPrisma.prisma = prisma
```

- [ ] **Step 2: Supabase server client**

Create `src/lib/supabase-server.ts`:

```typescript
import { createServerClient } from '@supabase/ssr'
import { cookies } from 'next/headers'

export function createSupabaseServerClient() {
  const cookieStore = cookies()
  return createServerClient(
    process.env.NEXT_PUBLIC_SUPABASE_URL!,
    process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!,
    {
      cookies: {
        get(name) { return cookieStore.get(name)?.value },
        set(name, value, options) { cookieStore.set({ name, value, ...options }) },
        remove(name, options) { cookieStore.set({ name, value: '', ...options }) },
      },
    }
  )
}
```

- [ ] **Step 3: Supabase browser client**

Create `src/lib/supabase-browser.ts`:

```typescript
import { createBrowserClient } from '@supabase/ssr'

export function createSupabaseBrowserClient() {
  return createBrowserClient(
    process.env.NEXT_PUBLIC_SUPABASE_URL!,
    process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!
  )
}
```

- [ ] **Step 4: Commit**

```bash
git add src/lib/
git commit -m "feat: add Prisma singleton and Supabase clients"
```

---

## Task 5: Authentication — Middleware + Login

**Files:**
- Create: `src/middleware.ts`, `src/app/(auth)/login/page.tsx`, `src/app/(operario)/layout.tsx`, `src/app/(supervisor)/layout.tsx`

- [ ] **Step 1: Auth middleware**

Create `src/middleware.ts`:

```typescript
import { createServerClient } from '@supabase/ssr'
import { NextResponse, type NextRequest } from 'next/server'

export async function middleware(request: NextRequest) {
  let response = NextResponse.next({ request: { headers: request.headers } })

  const supabase = createServerClient(
    process.env.NEXT_PUBLIC_SUPABASE_URL!,
    process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!,
    {
      cookies: {
        get(name) { return request.cookies.get(name)?.value },
        set(name, value, options) {
          request.cookies.set({ name, value, ...options })
          response = NextResponse.next({ request: { headers: request.headers } })
          response.cookies.set({ name, value, ...options })
        },
        remove(name, options) {
          request.cookies.set({ name, value: '', ...options })
          response = NextResponse.next({ request: { headers: request.headers } })
          response.cookies.set({ name, value: '', ...options })
        },
      },
    }
  )

  const { data: { user } } = await supabase.auth.getUser()

  if (!user && !request.nextUrl.pathname.startsWith('/login')) {
    return NextResponse.redirect(new URL('/login', request.url))
  }

  return response
}

export const config = {
  matcher: ['/((?!_next/static|_next/image|favicon.ico).*)'],
}
```

- [ ] **Step 2: Login page**

Create `src/app/(auth)/login/page.tsx`:

```typescript
'use client'
import { createSupabaseBrowserClient } from '@/lib/supabase-browser'
import { useState } from 'react'
import { useRouter } from 'next/navigation'

export default function LoginPage() {
  const [email, setEmail] = useState('')
  const [password, setPassword] = useState('')
  const [error, setError] = useState('')
  const router = useRouter()
  const supabase = createSupabaseBrowserClient()

  async function handleLogin(e: React.FormEvent) {
    e.preventDefault()
    const { error } = await supabase.auth.signInWithPassword({ email, password })
    if (error) { setError(error.message); return }
    router.push('/')
  }

  return (
    <div className="min-h-screen flex items-center justify-center bg-gray-950">
      <form onSubmit={handleLogin} className="bg-gray-900 p-8 rounded-xl w-80 flex flex-col gap-4">
        <h1 className="text-white text-2xl font-bold">VELUM</h1>
        <input
          type="email" placeholder="Email" value={email}
          onChange={e => setEmail(e.target.value)}
          className="bg-gray-800 text-white px-4 py-2 rounded-lg"
        />
        <input
          type="password" placeholder="Contraseña" value={password}
          onChange={e => setPassword(e.target.value)}
          className="bg-gray-800 text-white px-4 py-2 rounded-lg"
        />
        {error && <p className="text-red-400 text-sm">{error}</p>}
        <button type="submit" className="bg-green-600 text-white py-2 rounded-lg font-semibold">
          Ingresar
        </button>
      </form>
    </div>
  )
}
```

- [ ] **Step 3: Role guards**

Create `src/app/(operario)/layout.tsx`:

```typescript
import { createSupabaseServerClient } from '@/lib/supabase-server'
import { prisma } from '@/lib/prisma'
import { redirect } from 'next/navigation'

export default async function OperarioLayout({ children }: { children: React.ReactNode }) {
  const supabase = createSupabaseServerClient()
  const { data: { user } } = await supabase.auth.getUser()
  if (!user) redirect('/login')

  const usuario = await prisma.usuario.findUnique({ where: { email: user.email! } })
  if (!usuario || usuario.rol === 'GERENCIA') redirect('/dashboard')

  return <>{children}</>
}
```

Create `src/app/(supervisor)/layout.tsx`:

```typescript
import { createSupabaseServerClient } from '@/lib/supabase-server'
import { prisma } from '@/lib/prisma'
import { redirect } from 'next/navigation'

export default async function SupervisorLayout({ children }: { children: React.ReactNode }) {
  const supabase = createSupabaseServerClient()
  const { data: { user } } = await supabase.auth.getUser()
  if (!user) redirect('/login')

  const usuario = await prisma.usuario.findUnique({ where: { email: user.email! } })
  if (!usuario || usuario.rol === 'OPERARIO') redirect('/')

  return <>{children}</>
}
```

- [ ] **Step 4: Commit**

```bash
git add src/middleware.ts src/app/
git commit -m "feat: add Supabase auth middleware and login page"
```

---

## Task 6: Cascade Engine (TDD)

**Files:**
- Create: `src/lib/cascada.ts`, `tests/lib/cascada.test.ts`

- [ ] **Step 1: Write failing tests first**

Create `tests/lib/cascada.test.ts`:

```typescript
import { describe, it, expect } from 'vitest'
import { evaluarCascada } from '../../src/lib/cascada'

const etapasBase = [
  { id: 'ej1', ordenSecuencia: 1, umbralActivacion: 0,   porcentajeActual: 0, estado: 'ACTIVA'   as const, etapaRutaId: 'er1', maquinaId: 'm1' },
  { id: 'ej2', ordenSecuencia: 2, umbralActivacion: 50,  porcentajeActual: 0, estado: 'PENDIENTE' as const, etapaRutaId: 'er2', maquinaId: 'm2' },
  { id: 'ej3', ordenSecuencia: 3, umbralActivacion: 40,  porcentajeActual: 0, estado: 'PENDIENTE' as const, etapaRutaId: 'er3', maquinaId: 'm3' },
  { id: 'ej4', ordenSecuencia: 4, umbralActivacion: 100, porcentajeActual: 0, estado: 'PENDIENTE' as const, etapaRutaId: 'er4', maquinaId: 'm4' },
]

describe('evaluarCascada', () => {
  it('no activa la siguiente etapa si no se alcanza el umbral', () => {
    const result = evaluarCascada(etapasBase, 'ej1', 40)
    expect(result.etapasAActivar).toEqual([])
  })

  it('activa la siguiente etapa exactamente al alcanzar el umbral', () => {
    const result = evaluarCascada(etapasBase, 'ej1', 50)
    expect(result.etapasAActivar).toContain('er2')
    expect(result.etapasAActivar).not.toContain('er3')
  })

  it('no activa una etapa que ya está ACTIVA', () => {
    const etapas = etapasBase.map((e, i) =>
      i === 1 ? { ...e, estado: 'ACTIVA' as const, porcentajeActual: 20 } : e
    )
    const result = evaluarCascada(etapas, 'ej1', 80)
    expect(result.etapasAActivar).not.toContain('er2')
  })

  it('activa en cascada si la etapa recién activada ya supera el siguiente umbral', () => {
    // ej2 ya está al 40% (activa), actualizamos ej1 a 80% → activa ej2→ej3 en cascada
    const etapas = [
      { ...etapasBase[0], porcentajeActual: 80, estado: 'ACTIVA' as const },
      { ...etapasBase[1], porcentajeActual: 40, estado: 'ACTIVA' as const },
      { ...etapasBase[2] },
      { ...etapasBase[3] },
    ]
    // ej2 ya activa y en 40% ≥ umbral de ej3 (40) → ej3 debe activarse
    const result = evaluarCascada(etapas, 'ej2', 40)
    expect(result.etapasAActivar).toContain('er3')
  })

  it('calcula porcentajeGlobal como promedio de todas las etapas', () => {
    // ej1=60, ej2=0, ej3=0, ej4=0 → 60/4 = 15
    const result = evaluarCascada(etapasBase, 'ej1', 60)
    expect(result.porcentajeGlobal).toBeCloseTo(15)
  })

  it('no activa etapas COMPLETADAS', () => {
    const etapas = etapasBase.map((e, i) =>
      i === 1 ? { ...e, estado: 'COMPLETADA' as const, porcentajeActual: 100 } : e
    )
    const result = evaluarCascada(etapas, 'ej1', 80)
    expect(result.etapasAActivar).not.toContain('er2')
  })
})
```

- [ ] **Step 2: Run tests — verify they fail**

```bash
npm run test:run -- tests/lib/cascada.test.ts
```

Expected: FAIL — `evaluarCascada` not found.

- [ ] **Step 3: Implement cascade engine**

Create `src/lib/cascada.ts`:

```typescript
export interface EtapaProgreso {
  id: string
  ordenSecuencia: number
  umbralActivacion: number
  porcentajeActual: number
  estado: 'PENDIENTE' | 'ACTIVA' | 'EN_ESPERA' | 'COMPLETADA'
  etapaRutaId: string
  maquinaId: string
}

export interface CascadaResult {
  etapasAActivar: string[]   // etapaRutaId[] de etapas recién habilitadas
  porcentajeGlobal: number
}

export function evaluarCascada(
  etapas: EtapaProgreso[],
  ejecucionIdActualizada: string,
  nuevoPorcentaje: number
): CascadaResult {
  const ordenadas = [...etapas].sort((a, b) => a.ordenSecuencia - b.ordenSecuencia)

  const actualizadas = ordenadas.map(e =>
    e.id === ejecucionIdActualizada ? { ...e, porcentajeActual: nuevoPorcentaje } : e
  )

  const etapasAActivar: string[] = []

  for (let i = 0; i < actualizadas.length - 1; i++) {
    const actual   = actualizadas[i]
    const siguiente = actualizadas[i + 1]

    if (
      actual.estado === 'ACTIVA' &&
      actual.porcentajeActual >= siguiente.umbralActivacion &&
      siguiente.estado === 'PENDIENTE'
    ) {
      etapasAActivar.push(siguiente.etapaRutaId)
      actualizadas[i + 1] = { ...siguiente, estado: 'ACTIVA' }
    }
  }

  const porcentajeGlobal =
    actualizadas.reduce((acc, e) => acc + e.porcentajeActual, 0) / actualizadas.length

  return { etapasAActivar, porcentajeGlobal }
}
```

- [ ] **Step 4: Run tests — verify they pass**

```bash
npm run test:run -- tests/lib/cascada.test.ts
```

Expected: 6 tests PASS.

- [ ] **Step 5: Commit**

```bash
git add src/lib/cascada.ts tests/lib/cascada.test.ts
git commit -m "feat: cascade engine with full TDD coverage"
```

---

## Task 7: Shared Types

**Files:**
- Create: `src/types/index.ts`

- [ ] **Step 1: Write types**

Create `src/types/index.ts`:

```typescript
export type { Rol, EstadoOrden, EstadoEjecucion, EstadoMaquina, Unidad } from '@prisma/client'

export interface OrdenConEjecuciones {
  id: string
  sistema: string
  producto: string
  cantidad: number
  unidad: string
  porcentajeGlobal: number
  estado: string
  prioridad: number
  proyecto: { nombre: string; cliente: string } | null
  ejecuciones: EjecucionConDetalle[]
}

export interface EjecucionConDetalle {
  id: string
  porcentajeActual: number
  estado: string
  fechaInicio: Date | null
  maquina: { id: string; nombre: string; tipo: string }
  etapaRuta: { nombreEtapa: string; ordenSecuencia: number; umbralActivacion: number }
}

export interface RegistrarProgresoInput {
  ejecucionEtapaId: string
  cantidadRegistrada: number
  notas?: string
  fueOverride?: boolean
  motivoOverride?: string
}
```

- [ ] **Step 2: Commit**

```bash
git add src/types/
git commit -m "feat: add shared TypeScript types"
```

---

## Task 8: API — Proyectos

**Files:**
- Create: `src/app/api/proyectos/route.ts`, `src/app/api/proyectos/[id]/route.ts`

- [ ] **Step 1: List + Create**

Create `src/app/api/proyectos/route.ts`:

```typescript
import { NextResponse } from 'next/server'
import { prisma } from '@/lib/prisma'

export async function GET() {
  const proyectos = await prisma.proyecto.findMany({
    orderBy: { createdAt: 'desc' },
    include: { _count: { select: { ordenes: true } } },
  })
  return NextResponse.json(proyectos)
}

export async function POST(req: Request) {
  const body = await req.json()
  const { nombre, cliente, fechaEntrega } = body

  if (!nombre || !cliente || !fechaEntrega) {
    return NextResponse.json({ error: 'nombre, cliente y fechaEntrega son requeridos' }, { status: 400 })
  }

  const proyecto = await prisma.proyecto.create({
    data: { nombre, cliente, fechaEntrega: new Date(fechaEntrega) },
  })
  return NextResponse.json(proyecto, { status: 201 })
}
```

- [ ] **Step 2: Get + Update single**

Create `src/app/api/proyectos/[id]/route.ts`:

```typescript
import { NextResponse } from 'next/server'
import { prisma } from '@/lib/prisma'

export async function GET(_req: Request, { params }: { params: { id: string } }) {
  const proyecto = await prisma.proyecto.findUnique({
    where: { id: params.id },
    include: { ordenes: { include: { ejecuciones: { include: { maquina: true, etapaRuta: true } } } } },
  })
  if (!proyecto) return NextResponse.json({ error: 'No encontrado' }, { status: 404 })
  return NextResponse.json(proyecto)
}

export async function PATCH(req: Request, { params }: { params: { id: string } }) {
  const body = await req.json()
  const proyecto = await prisma.proyecto.update({
    where: { id: params.id },
    data: body,
  })
  return NextResponse.json(proyecto)
}
```

- [ ] **Step 3: Commit**

```bash
git add src/app/api/proyectos/
git commit -m "feat: proyectos CRUD API"
```

---

## Task 9: API — Órdenes + Registro de Progreso

**Files:**
- Create: `src/app/api/ordenes/route.ts`, `src/app/api/ordenes/[id]/route.ts`, `src/app/api/ordenes/[id]/progreso/route.ts`

- [ ] **Step 1: List + Create order (auto-assigns route)**

Create `src/app/api/ordenes/route.ts`:

```typescript
import { NextResponse } from 'next/server'
import { prisma } from '@/lib/prisma'

export async function GET() {
  const ordenes = await prisma.ordenProduccion.findMany({
    where: { estado: { not: 'CANCELADA' } },
    orderBy: [{ prioridad: 'desc' }, { createdAt: 'desc' }],
    include: {
      proyecto: { select: { nombre: true, cliente: true } },
      ejecuciones: {
        include: { maquina: true, etapaRuta: true },
        orderBy: { etapaRuta: { ordenSecuencia: 'asc' } },
      },
    },
  })
  return NextResponse.json(ordenes)
}

export async function POST(req: Request) {
  const body = await req.json()
  const { sistema, producto, cantidad, unidad, proyectoId } = body

  if (!sistema || !producto || !cantidad || !unidad) {
    return NextResponse.json({ error: 'sistema, producto, cantidad y unidad son requeridos' }, { status: 400 })
  }

  const ruta = await prisma.ruta.findUnique({
    where: { sistema_producto: { sistema, producto } },
    include: { etapas: { orderBy: { ordenSecuencia: 'asc' }, include: { maquina: true } } },
  })

  if (!ruta) {
    return NextResponse.json({ error: `Ruta no encontrada para ${sistema}/${producto}` }, { status: 404 })
  }

  const orden = await prisma.$transaction(async (tx) => {
    const nuevaOrden = await tx.ordenProduccion.create({
      data: { sistema, producto, cantidad, unidad, rutaId: ruta.id, proyectoId: proyectoId ?? null, estado: 'EN_PRODUCCION' },
    })

    // Create only the first stage as ACTIVA; rest stay PENDIENTE
    await tx.ejecucionEtapa.createMany({
      data: ruta.etapas.map((etapa, idx) => ({
        ordenId:     nuevaOrden.id,
        etapaRutaId: etapa.id,
        maquinaId:   etapa.maquinaId,
        estado:      idx === 0 ? 'ACTIVA' : 'PENDIENTE',
        fechaInicio: idx === 0 ? new Date() : null,
      })),
    })

    return nuevaOrden
  })

  return NextResponse.json(orden, { status: 201 })
}
```

- [ ] **Step 2: Get single order**

Create `src/app/api/ordenes/[id]/route.ts`:

```typescript
import { NextResponse } from 'next/server'
import { prisma } from '@/lib/prisma'

export async function GET(_req: Request, { params }: { params: { id: string } }) {
  const orden = await prisma.ordenProduccion.findUnique({
    where: { id: params.id },
    include: {
      proyecto: true,
      ejecuciones: {
        include: { maquina: true, etapaRuta: true },
        orderBy: { etapaRuta: { ordenSecuencia: 'asc' } },
      },
    },
  })
  if (!orden) return NextResponse.json({ error: 'No encontrada' }, { status: 404 })
  return NextResponse.json(orden)
}

export async function PATCH(req: Request, { params }: { params: { id: string } }) {
  const body = await req.json()
  const orden = await prisma.ordenProduccion.update({ where: { id: params.id }, data: body })
  return NextResponse.json(orden)
}
```

- [ ] **Step 3: Register progress + cascade trigger**

Create `src/app/api/ordenes/[id]/progreso/route.ts`:

```typescript
import { NextResponse } from 'next/server'
import { prisma } from '@/lib/prisma'
import { createSupabaseServerClient } from '@/lib/supabase-server'
import { evaluarCascada, EtapaProgreso } from '@/lib/cascada'

export async function POST(req: Request, { params }: { params: { id: string } }) {
  const body = await req.json()
  const { ejecucionEtapaId, cantidadRegistrada, notas, fueOverride, motivoOverride } = body

  if (!ejecucionEtapaId || cantidadRegistrada == null) {
    return NextResponse.json({ error: 'ejecucionEtapaId y cantidadRegistrada son requeridos' }, { status: 400 })
  }

  const supabase = createSupabaseServerClient()
  const { data: { user } } = await supabase.auth.getUser()
  if (!user) return NextResponse.json({ error: 'No autorizado' }, { status: 401 })

  const usuario = await prisma.usuario.findUnique({ where: { email: user.email! } })
  if (!usuario) return NextResponse.json({ error: 'Usuario no encontrado' }, { status: 404 })

  // Load all executions for this order to evaluate cascade
  const ejecuciones = await prisma.ejecucionEtapa.findMany({
    where: { ordenId: params.id },
    include: { etapaRuta: true },
    orderBy: { etapaRuta: { ordenSecuencia: 'asc' } },
  })

  const ordenData = await prisma.ordenProduccion.findUnique({ where: { id: params.id } })
  if (!ordenData) return NextResponse.json({ error: 'Orden no encontrada' }, { status: 404 })

  const ejecucionActual = ejecuciones.find(e => e.id === ejecucionEtapaId)
  if (!ejecucionActual) return NextResponse.json({ error: 'Ejecución no encontrada' }, { status: 404 })

  const nuevaCantidadTotal = ejecucionActual.porcentajeActual / 100 * ordenData.cantidad + cantidadRegistrada
  const nuevoPorcentaje = Math.min((nuevaCantidadTotal / ordenData.cantidad) * 100, 100)

  const etapasProgreso: EtapaProgreso[] = ejecuciones.map(e => ({
    id:               e.id,
    ordenSecuencia:   e.etapaRuta.ordenSecuencia,
    umbralActivacion: e.etapaRuta.umbralActivacion,
    porcentajeActual: e.porcentajeActual,
    estado:           e.estado as EtapaProgreso['estado'],
    etapaRutaId:      e.etapaRutaId,
    maquinaId:        e.maquinaId,
  }))

  const { etapasAActivar, porcentajeGlobal } = evaluarCascada(etapasProgreso, ejecucionEtapaId, nuevoPorcentaje)

  await prisma.$transaction(async (tx) => {
    // Save progress log
    await tx.registroProgreso.create({
      data: {
        ejecucionEtapaId,
        usuarioId: usuario.id,
        cantidadRegistrada,
        porcentajeRegistrado: nuevoPorcentaje,
        notas,
        fueOverride: fueOverride ?? false,
        motivoOverride,
      },
    })

    // Update current execution percentage
    await tx.ejecucionEtapa.update({
      where: { id: ejecucionEtapaId },
      data: {
        porcentajeActual: nuevoPorcentaje,
        estado: nuevoPorcentaje >= 100 ? 'COMPLETADA' : 'ACTIVA',
        fechaFin: nuevoPorcentaje >= 100 ? new Date() : null,
      },
    })

    // Activate cascaded stages
    if (etapasAActivar.length > 0) {
      await tx.ejecucionEtapa.updateMany({
        where: { ordenId: params.id, etapaRutaId: { in: etapasAActivar } },
        data: { estado: 'ACTIVA', fechaInicio: new Date() },
      })
    }

    // Recalculate global percentage
    await tx.ordenProduccion.update({
      where: { id: params.id },
      data: { porcentajeGlobal },
    })
  })

  // Broadcast update via Supabase Realtime
  const { createClient } = await import('@supabase/supabase-js')
  const adminClient = createClient(
    process.env.NEXT_PUBLIC_SUPABASE_URL!,
    process.env.SUPABASE_SERVICE_ROLE_KEY!
  )
  await adminClient.channel('ordenes').send({
    type: 'broadcast',
    event: 'progreso',
    payload: { ordenId: params.id, porcentajeGlobal, etapasActivadas: etapasAActivar },
  })

  return NextResponse.json({ porcentajeGlobal, etapasActivadas: etapasAActivar })
}
```

- [ ] **Step 4: Commit**

```bash
git add src/app/api/ordenes/
git commit -m "feat: orders API with cascade trigger and Realtime broadcast"
```

---

## Task 10: API — Máquinas

**Files:**
- Create: `src/app/api/maquinas/route.ts`, `src/app/api/maquinas/[id]/route.ts`

- [ ] **Step 1: List machines**

Create `src/app/api/maquinas/route.ts`:

```typescript
import { NextResponse } from 'next/server'
import { prisma } from '@/lib/prisma'

export async function GET() {
  const maquinas = await prisma.maquina.findMany({ orderBy: { nombre: 'asc' } })
  return NextResponse.json(maquinas)
}
```

- [ ] **Step 2: Update machine status**

Create `src/app/api/maquinas/[id]/route.ts`:

```typescript
import { NextResponse } from 'next/server'
import { prisma } from '@/lib/prisma'
import { EstadoMaquina } from '@prisma/client'

export async function PATCH(req: Request, { params }: { params: { id: string } }) {
  const { estadoActual } = await req.json()

  if (!Object.values(EstadoMaquina).includes(estadoActual)) {
    return NextResponse.json({ error: 'Estado inválido' }, { status: 400 })
  }

  const maquina = await prisma.maquina.update({
    where: { id: params.id },
    data: { estadoActual },
  })

  // If machine goes down, put waiting executions on hold
  if (estadoActual !== 'OPERATIVA') {
    await prisma.ejecucionEtapa.updateMany({
      where: { maquinaId: params.id, estado: 'ACTIVA' },
      data: { estado: 'EN_ESPERA' },
    })
  } else {
    // Machine restored — reactivate waiting executions
    await prisma.ejecucionEtapa.updateMany({
      where: { maquinaId: params.id, estado: 'EN_ESPERA' },
      data: { estado: 'ACTIVA' },
    })
  }

  return NextResponse.json(maquina)
}
```

- [ ] **Step 3: Commit**

```bash
git add src/app/api/maquinas/
git commit -m "feat: machines API with status management"
```

---

## Task 11: Vista Operario (mobile-first)

**Files:**
- Create: `src/app/(operario)/page.tsx`, `src/components/operario/OrdenCard.tsx`, `src/components/operario/RegistrarAvanceForm.tsx`

- [ ] **Step 1: Machine selector + active orders page**

Create `src/app/(operario)/page.tsx`:

```typescript
import { createSupabaseServerClient } from '@/lib/supabase-server'
import { prisma } from '@/lib/prisma'
import OrdenCard from '@/components/operario/OrdenCard'

export default async function OperarioPage() {
  const supabase = createSupabaseServerClient()
  const { data: { user } } = await supabase.auth.getUser()
  const usuario = await prisma.usuario.findUnique({ where: { email: user!.email! } })

  const ejecucionesActivas = await prisma.ejecucionEtapa.findMany({
    where: { estado: 'ACTIVA' },
    include: {
      orden: { include: { proyecto: true } },
      maquina: true,
      etapaRuta: true,
    },
    orderBy: [{ orden: { prioridad: 'desc' } }, { createdAt: 'asc' }],
  })

  return (
    <main className="min-h-screen bg-gray-950 p-4">
      <div className="flex justify-between items-center mb-6">
        <h1 className="text-white text-xl font-bold">VELUM · Producción</h1>
        <span className="text-gray-400 text-sm">{usuario?.nombre}</span>
      </div>

      {ejecucionesActivas.length === 0 ? (
        <p className="text-gray-500 text-center mt-16">No hay etapas activas en este momento.</p>
      ) : (
        <div className="flex flex-col gap-4">
          {ejecucionesActivas.map(ej => (
            <OrdenCard key={ej.id} ejecucion={ej} usuarioId={usuario!.id} />
          ))}
        </div>
      )}
    </main>
  )
}
```

- [ ] **Step 2: Order card with inline progress form**

Create `src/components/operario/OrdenCard.tsx`:

```typescript
'use client'
import { useState } from 'react'
import { useRouter } from 'next/navigation'

interface Props {
  ejecucion: any
  usuarioId: string
}

export default function OrdenCard({ ejecucion, usuarioId }: Props) {
  const [cantidad, setCantidad] = useState(10)
  const [loading, setLoading] = useState(false)
  const router = useRouter()

  const orden = ejecucion.orden
  const prioridad = orden.prioridad > 0

  async function registrar() {
    setLoading(true)
    await fetch(`/api/ordenes/${orden.id}/progreso`, {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ ejecucionEtapaId: ejecucion.id, cantidadRegistrada: cantidad }),
    })
    setLoading(false)
    router.refresh()
  }

  return (
    <div className={`bg-gray-900 rounded-xl p-4 border-l-4 ${prioridad ? 'border-red-500' : 'border-green-500'}`}>
      {prioridad && <span className="text-red-400 text-xs font-bold mb-2 block">🚨 URGENTE</span>}

      <div className="flex justify-between items-start mb-3">
        <div>
          <p className="text-white font-bold">{orden.sistema} / {orden.producto}</p>
          <p className="text-gray-400 text-sm">{orden.proyecto?.nombre ?? 'Orden manual'}</p>
        </div>
        <span className="text-gray-500 text-xs">{ejecucion.maquina.nombre}</span>
      </div>

      <div className="mb-4">
        <div className="flex justify-between text-sm mb-1">
          <span className="text-gray-400">{ejecucion.etapaRuta.nombreEtapa}</span>
          <span className="text-green-400 font-bold">{ejecucion.porcentajeActual.toFixed(0)}%</span>
        </div>
        <div className="bg-gray-700 rounded-full h-2">
          <div className="bg-green-500 h-2 rounded-full transition-all" style={{ width: `${ejecucion.porcentajeActual}%` }} />
        </div>
        <p className="text-gray-500 text-xs mt-1">Umbral siguiente etapa: {ejecucion.etapaRuta.umbralActivacion}%</p>
      </div>

      <div className="flex items-center gap-3">
        <button onClick={() => setCantidad(Math.max(1, cantidad - 10))} className="bg-gray-700 text-white w-10 h-10 rounded-lg text-xl">−</button>
        <div className="flex-1 text-center">
          <span className="text-white text-2xl font-bold">{cantidad}</span>
          <p className="text-gray-500 text-xs">{orden.unidad.toLowerCase()}</p>
        </div>
        <button onClick={() => setCantidad(cantidad + 10)} className="bg-gray-700 text-white w-10 h-10 rounded-lg text-xl">+</button>
        <button onClick={registrar} disabled={loading} className="flex-1 bg-green-600 text-white py-2 rounded-lg font-bold disabled:opacity-50">
          {loading ? '...' : 'Registrar'}
        </button>
      </div>
    </div>
  )
}
```

- [ ] **Step 3: Commit**

```bash
git add src/app/\(operario\)/ src/components/operario/
git commit -m "feat: operario mobile view with inline progress registration"
```

---

## Task 12: Vista Supervisor — Dashboard en Tiempo Real

**Files:**
- Create: `src/app/(supervisor)/dashboard/page.tsx`, `src/components/supervisor/OrdenCascadaCard.tsx`, `src/components/supervisor/MaquinasStatus.tsx`, `src/components/supervisor/RealtimeListener.tsx`

- [ ] **Step 1: Dashboard page (server)**

Create `src/app/(supervisor)/dashboard/page.tsx`:

```typescript
import { prisma } from '@/lib/prisma'
import OrdenCascadaCard from '@/components/supervisor/OrdenCascadaCard'
import MaquinasStatus from '@/components/supervisor/MaquinasStatus'
import RealtimeListener from '@/components/supervisor/RealtimeListener'

export const dynamic = 'force-dynamic'

export default async function SupervisorDashboard() {
  const [ordenes, maquinas] = await Promise.all([
    prisma.ordenProduccion.findMany({
      where: { estado: { in: ['EN_PRODUCCION', 'EN_ESPERA'] } },
      orderBy: [{ prioridad: 'desc' }, { createdAt: 'desc' }],
      include: {
        proyecto: true,
        ejecuciones: {
          include: { maquina: true, etapaRuta: true },
          orderBy: { etapaRuta: { ordenSecuencia: 'asc' } },
        },
      },
    }),
    prisma.maquina.findMany({ orderBy: { nombre: 'asc' } }),
  ])

  return (
    <main className="min-h-screen bg-gray-950 p-6">
      <RealtimeListener />
      <h1 className="text-white text-2xl font-bold mb-6">VELUM · Planta en vivo</h1>

      <div className="grid grid-cols-1 xl:grid-cols-4 gap-6">
        <div className="xl:col-span-3 flex flex-col gap-4">
          {ordenes.length === 0 ? (
            <p className="text-gray-500">No hay órdenes activas.</p>
          ) : (
            ordenes.map(orden => <OrdenCascadaCard key={orden.id} orden={orden as any} />)
          )}
        </div>
        <div>
          <MaquinasStatus maquinas={maquinas} />
        </div>
      </div>
    </main>
  )
}
```

- [ ] **Step 2: Order cascade card**

Create `src/components/supervisor/OrdenCascadaCard.tsx`:

```typescript
import type { OrdenConEjecuciones } from '@/types'

const ESTADO_COLOR: Record<string, string> = {
  ACTIVA:     'bg-green-600 text-green-100',
  PENDIENTE:  'bg-gray-700 text-gray-400',
  EN_ESPERA:  'bg-yellow-700 text-yellow-200',
  COMPLETADA: 'bg-blue-800 text-blue-200',
}

export default function OrdenCascadaCard({ orden }: { orden: OrdenConEjecuciones }) {
  const borderColor = orden.prioridad > 0 ? 'border-red-500' : 'border-green-800'

  return (
    <div className={`bg-gray-900 rounded-xl p-4 border-l-4 ${borderColor}`}>
      <div className="flex justify-between items-center mb-3">
        <div>
          <span className="text-white font-bold">{orden.sistema} / {orden.producto}</span>
          <span className="text-gray-400 text-sm ml-3">{orden.proyecto?.nombre ?? 'Manual'}</span>
        </div>
        <span className="text-green-400 font-bold">{orden.porcentajeGlobal.toFixed(0)}%</span>
      </div>

      <div className="flex flex-wrap gap-2 mb-3">
        {orden.ejecuciones.map(ej => (
          <div key={ej.id} className={`px-2 py-1 rounded text-xs font-medium ${ESTADO_COLOR[ej.estado]}`}>
            {ej.etapaRuta.nombreEtapa}
            {ej.estado === 'ACTIVA' && ` ${ej.porcentajeActual.toFixed(0)}%`}
            {ej.estado === 'PENDIENTE' && ` ⏳${ej.etapaRuta.umbralActivacion}%`}
          </div>
        ))}
      </div>

      <div className="bg-gray-700 rounded-full h-1.5">
        <div className="bg-green-500 h-1.5 rounded-full transition-all" style={{ width: `${orden.porcentajeGlobal}%` }} />
      </div>
    </div>
  )
}
```

- [ ] **Step 3: Machines status panel**

Create `src/components/supervisor/MaquinasStatus.tsx`:

```typescript
'use client'
import { useState } from 'react'
import type { Maquina } from '@prisma/client'

const ESTADO_STYLE: Record<string, string> = {
  OPERATIVA:         'bg-green-900 text-green-300 border-green-700',
  MANTENIMIENTO:     'bg-yellow-900 text-yellow-300 border-yellow-700',
  FUERA_DE_SERVICIO: 'bg-red-900 text-red-300 border-red-700',
}

export default function MaquinasStatus({ maquinas }: { maquinas: Maquina[] }) {
  const [lista, setLista] = useState(maquinas)

  async function toggleEstado(id: string, estadoActual: string) {
    const siguiente = estadoActual === 'OPERATIVA' ? 'MANTENIMIENTO' : 'OPERATIVA'
    await fetch(`/api/maquinas/${id}`, {
      method: 'PATCH',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ estadoActual: siguiente }),
    })
    setLista(prev => prev.map(m => m.id === id ? { ...m, estadoActual: siguiente as any } : m))
  }

  return (
    <div className="bg-gray-900 rounded-xl p-4">
      <h3 className="text-gray-300 font-semibold mb-4 text-sm uppercase tracking-wide">Máquinas</h3>
      <div className="flex flex-col gap-2">
        {lista.map(m => (
          <button
            key={m.id}
            onClick={() => toggleEstado(m.id, m.estadoActual)}
            className={`text-left px-3 py-2 rounded-lg border text-xs font-medium ${ESTADO_STYLE[m.estadoActual]}`}
          >
            {m.nombre}
            <span className="float-right opacity-60">{m.estadoActual === 'OPERATIVA' ? '●' : '⚠'}</span>
          </button>
        ))}
      </div>
    </div>
  )
}
```

- [ ] **Step 4: Realtime listener (auto-refresh on cascade events)**

Create `src/components/supervisor/RealtimeListener.tsx`:

```typescript
'use client'
import { useEffect } from 'react'
import { useRouter } from 'next/navigation'
import { createSupabaseBrowserClient } from '@/lib/supabase-browser'

export default function RealtimeListener() {
  const router = useRouter()
  const supabase = createSupabaseBrowserClient()

  useEffect(() => {
    const channel = supabase
      .channel('ordenes')
      .on('broadcast', { event: 'progreso' }, () => {
        router.refresh()
      })
      .subscribe()

    return () => { supabase.removeChannel(channel) }
  }, [router, supabase])

  return null
}
```

- [ ] **Step 5: Commit**

```bash
git add src/app/\(supervisor\)/ src/components/supervisor/
git commit -m "feat: supervisor real-time dashboard with cascade visualization"
```

---

## Task 13: Root page redirect + final wiring

**Files:**
- Modify: `src/app/page.tsx`

- [ ] **Step 1: Root redirect by role**

Replace `src/app/page.tsx`:

```typescript
import { createSupabaseServerClient } from '@/lib/supabase-server'
import { prisma } from '@/lib/prisma'
import { redirect } from 'next/navigation'

export default async function RootPage() {
  const supabase = createSupabaseServerClient()
  const { data: { user } } = await supabase.auth.getUser()

  if (!user) redirect('/login')

  const usuario = await prisma.usuario.findUnique({ where: { email: user.email! } })

  if (!usuario) redirect('/login')
  if (usuario.rol === 'OPERARIO') redirect('/(operario)')

  redirect('/dashboard')
}
```

- [ ] **Step 2: Final integration test**

Create a test user in Supabase dashboard → Authentication → Users. Then in Supabase SQL editor run:

```sql
INSERT INTO "Usuario" (id, nombre, email, rol, "createdAt")
VALUES (gen_random_uuid(), 'Supervisor Test', 'supervisor@velum.com', 'SUPERVISOR', now());

INSERT INTO "Usuario" (id, nombre, email, rol, "createdAt")
VALUES (gen_random_uuid(), 'Operario Test', 'operario@velum.com', 'OPERARIO', now());
```

- [ ] **Step 3: End-to-end smoke test**

```
1. Login como supervisor@velum.com → debe redirigir a /dashboard
2. Crear proyecto: POST /api/proyectos { nombre: "Test", cliente: "Cliente1", fechaEntrega: "2026-12-31" }
3. Crear orden: POST /api/ordenes { sistema: "Sheet", producto: "MultiSlim.A", cantidad: 100, unidad: "PIEZAS", proyectoId: <id> }
4. Verificar que la orden aparece en el dashboard con Punzonado=ACTIVA y el resto PENDIENTE
5. Login como operario@velum.com → debe ver la orden con la etapa Punzonado
6. Registrar 50 piezas → porcentaje debe subir a 50%
7. Verificar que Plegado pasa a ACTIVA automáticamente en el dashboard del supervisor
```

- [ ] **Step 4: Deploy to Vercel**

```bash
npx vercel --prod
```

Add environment variables in Vercel dashboard (same as `.env.local`).

- [ ] **Step 5: Final commit**

```bash
git add src/app/page.tsx
git commit -m "feat: root redirect by user role — Fase 1 MVP complete"
git push origin master
```

---

## Spec Coverage Check

| Requisito spec | Task |
|----------------|------|
| Auth y roles (operario/supervisor/gerencia) | Task 5 |
| CRUD proyectos | Task 8 |
| CRUD órdenes + asignación automática de ruta | Task 9 |
| Rutas precargadas (seed data) | Task 3 |
| Motor de cascada con umbrales | Task 6 + Task 9 |
| Vista operario mobile | Task 11 |
| Vista supervisor tiempo real | Task 12 |
| Máquinas: estado operativa/mantenimiento | Task 10 |
| Registro de progreso inmutable (trazabilidad) | Task 9 |
| Override de supervisor con motivo | Task 9 (campo `fueOverride`) |
| Orden urgente / prioridad | Task 11 + Task 12 (UI) |
| Cierre de turno / persistencia entre turnos | Task 9 (estado persiste en DB) |
| WebSocket push en tiempo real | Task 9 (broadcast) + Task 12 (listener) |
| porcentajeGlobal = promedio ponderado | Task 6 (cascada.ts) + Task 9 |
